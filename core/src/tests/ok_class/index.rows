import .outside;

function log(m: string);

class N {
    n: number;
}

const n0 = new N(42);

function foo() {
    log("foo");
}

class Foo {
    n: number;

    update() {
        this.n = this.n + 1;
    }

    foo() {
        this.update();
        log("Foo::foo");
    }
}

new Foo(42).foo();

function f() {
    log(new ::outside::Outside("hello").outside());
}
