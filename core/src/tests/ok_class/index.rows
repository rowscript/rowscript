function log();

class Foo {
    n: number;

    update() {
        this.n = this.n + 1;
    }

    foo() {
//        this.update(); // lol fails
        log();
    }
}

function f0() {
//    new Foo(42).foo(); // damn, it fails
}
