type AppID = string;

function f0(): AppID {
    "123"
}

type Optional<T> = [None | Ok(T)];

function f1(): Optional<number> {
    Ok(42)
}

type Person<T>;

console.assert(f0() == "123");
console.assert(f1().unwrap() == 42);
