type AppID = string;

function f0(): AppID {
    "123"
}

type Optional<T> = [None | Ok(T)];

function f1(): Optional<number> {
    Ok(42)
}

type Person<T>;

testing.assert(f0() == "123");
testing.assert(f1().unwrap() == 42);
