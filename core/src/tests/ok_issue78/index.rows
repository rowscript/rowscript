function log<T>(m: T);

class Foo<T> {
    data: T;
    foo(): T { this.data }
}

function f() {
    let a = new Foo<number>(42);
    log(a:foo());
}

// This should run after vtbl is initialized.
f();
