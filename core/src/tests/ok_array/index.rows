function f0(): number {
    const a = [];
    const m = a.push(42);
    const n = a.push(69);
    a.forEach((n) => console.log(n));
    a.length()
}
testing.assert(f0() == 2);

function f1(): number {
    const a = [1];
    a[0]
}
testing.assert(f1() == 1);

function f2(): number {
    const a = [1];
    a[0] = 2;
    a[0]
}
testing.assert(f2() == 2);

function f3(): string {
    const a = [["1"]];
    a[0][0]
}
testing.assert(f3() == "1");

function f4(): Array<string>;
testing.assert(f4()[0] == "a");
