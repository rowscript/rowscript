function log(n: number);

function f0() {
    const a = [];
    const m = a.push(42);
    const n = a.push(69);
    const f: (n: number) -> unit = (n) => log(n);
    a.forEach(f);
    log(a.length());
}

f0();

function f1(): number {
    const a = [1];
    a[0]
}

const n1 = f1();

function f2(): number {
    const a = [1];
    a[0] = 2;
    a[0]
}

const n2 = f2();
