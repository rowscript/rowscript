function rand(): number;

function f0(): number {
    number#__add__(1, 1)
}
testing.assert(f0() == 2);

function f1(): number {
    number#__add__(rand(), rand())
}
testing.assert(f1() == 2);

testing.assert(json#stringify({hello: 42}) == "{\"hello\":42}");
