function log<T>(v: T);

function f1<I: type -> type, T: type>(it: I<T>)
where Iterator<I>
{
    const n = it.next();
    log(n);
}

function f0() {
    const a = [42];
    const it = a.iter();
    f1<ArrayIterator, number>(it);
}

f0();
