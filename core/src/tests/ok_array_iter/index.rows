function f1<T>(it: T)
with Iterator<T>
{
    const a = it.next();
    testing.assert(a.isOk());
    const b = it.next();
    testing.assert(!b.isOk());
}

function f0() {
    const a = [42];
    const it = a.iter();
    f1(it);
}

f0();
