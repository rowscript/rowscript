/* Triple slash starts a comment group as the docstring. */

/// Empty function with an unspecified return type.
function f0() end

/// A global variable.
v0 := f0()

/// Function with parameters and a return type.
f1(a u64, b str) bool := false

v1 := f1(42, "hello")

/// This function shows all control flow constructs.
function f2(a)
    // A local variable.
    c u64 := 42

    // An if-guard statement.
    if c == 0
        return a
    end

    // An if statement.
    if c == 1
        return false
    else if c == 2
        return false
    else
        return v1
    end

    /// An inner function.
    function f3()
        x := 42
        return c
    end

    false
end
