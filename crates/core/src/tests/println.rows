inline function print<...Args: Display>(inline fmt: str, ...args: Args) {
    inline for (inline let i: usize = 0; i < args.len(); i += 1) {
        // TODO: Parse the `fmt` and write the strings.
        puts(args[i].toString())
    }
}

function main() {
    let name = "RowScript"
    let year = 2025
    print("Hello, {0} in {1}!", name, year)
    print("Hello, {} in {}!", name, year)
    print("Hello, {name} in {year}!")
}
