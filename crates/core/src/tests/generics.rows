function id<T>(a: T): T {
    return a
}

struct A<T> {
    a: T
}

extends<T> A<T> {
    foo<U>(a: T, b: U) {
    }
}

function get1<T>(x: A<T>): T {
    return x.a
}

function get2<T>(a: T): A<T> {
    return A<T>{a: a}
}

function main() {
    let x = A<u32>{a: 42}
    assert(x.a == 42)
    assert(get1<u32>(x) == 42)
    assert(get2<u32>(x.a).a == 42)
}

function f<...Args>() {}
