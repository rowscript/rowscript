struct A {
    n: u32
    m: i64
}

extends A {
    foo(i: u32): u32 {
        return this.n + i
    }
}

function main() {
    let a = A{n: 42, m: 69}
    assert(a.n == 42)
    assert(a.foo(69) == 111)
}
