[package]
name = "rowscript_core"
version = "0.1.0"
edition = "2024"

[dependencies]
strum = { version = "0.27.2", features = ["derive"] }
ustr = "1.1.0"
chumsky = {  version = "1.0.0-alpha.8", features = ["pratt"] }
