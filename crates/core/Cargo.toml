[package]
name = "rowscript-core"
version = "1.0.0-alpha.6"
edition = "2024"

[dependencies]
rowscript-derive = { path = "../derive" }
strum = { version = "0.28.0", features = ["derive"] }
ustr = "1.1.0"
serde_json = "1.0.150"
chumsky = { version = "1.0.0-alpha.8", features = ["pratt"] }
cranelift = "0.132.0"
cranelift-native = "0.132.0"
cranelift-module = "0.132.0"
cranelift-jit = "0.132.0"
object = { version = "0.39.1", features = ["write", "read"] }
target-lexicon = "0.13.5"
wasmtime-internal-jit-debug = { version = "45.0.0", features = ["std", "gdb_jit_int"] }

[dev-dependencies]
criterion = "0.8.2"

[[bench]]
name = "eval"
harness = false
